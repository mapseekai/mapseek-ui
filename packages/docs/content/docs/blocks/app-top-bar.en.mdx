---
title: "AppTopBar"
registryName: app-top-bar
category: block
stability: stable
showcase: app-top-bar
---

import { ShowcaseDemo } from "@/components/ShowcaseDemo"
import { RegistryDependencies, RegistryInstall } from "@/components/RegistryItem"


AppTopBar is the application chrome for map editors. It owns the layout for back, brand, project name, save, and action slots; the caller provides branding, save status, snapshot actions, routing, and persistence behavior.

## Installation

<RegistryInstall registryName="app-top-bar" />

## Import

```tsx
import {
  AppTopBar,
  type AppTopBarLabels,
  type AppTopBarProps,
  type AppTopBarSize,
} from "@/registry/blocks/app-top-bar"
```

## Controlled Save State

<ShowcaseDemo registryName="app-top-bar" title="Controlled save state" description="Shows brand slot, dirty status Tag, save, save-as, snapshot actions, and `xs`, `sm`, `default`, and `lg` size comparisons." minHeight={460} />

## Centered icon actions

<ShowcaseDemo registryName="app-top-bar" demo="center-actions" title="Centered icon actions" description="Shows a tooltip-enabled map-tool group placed at the top bar's geometric center through `centerActions`." minHeight={160} />

## Usage

Pass `projectName`, `brand`, `status`, and `labels` as display inputs, then connect `onBack` and `onSave` to app routing and persistence. Use `beforeSaveActions`, `afterSaveActions`, `centerActions`, and `endActions` for caller-owned export, history, snapshot, or sharing buttons.

When `centerActions` is present, equal outer desktop columns and a middle column keep the tool group geometrically centered. From `md` up to `lg`, brand and status temporarily collapse and the project name truncates before it can overlap the center controls.

## Sizes

`size` supports `xs`, `sm`, `default`, and `lg`; `default` is the default value. On desktop, every variant retains 4px of top and bottom shell padding:

| `size` | Built-in Button | Inner toolbar | Outer top bar |
| --- | ---: | ---: | ---: |
| `xs` | 24px | 24px | 32px |
| `sm` | 28px | 28px | 36px |
| `default` | 32px | 40px | 48px |
| `lg` | 36px | 48px | 56px |

The prop also scales the built-in back and Save buttons to their matching Button variants. Action slots, `brand`, and `status` remain caller-owned; action Buttons should use the same size tier.

## Public Exports and Props

Exports include `AppTopBar`, `AppTopBarProps`, `AppTopBarLabels`, and `AppTopBarSize`. Key props are `size`, `projectName`, `labels`, `onBack`, `onSave`, `saveDisabled`, `savePending`, `brand`, `status`, and the four action slots. The block does not include brand assets or status Tag styles.

## Controlled State

Save state belongs to the caller. In the example, Save clears dirty state, Save as marks it dirty again, and Snapshot only updates outer status text. Real apps usually bind `savePending` to an async save promise and show failures through an outer toast or Dialog.

## Accessibility

The back button uses `labels.back` as its accessible name, and the save button uses `labels.save`. When passing action slots, prefer native `button` or the project Button and provide `aria-label` or clear text for icon-only controls. `status` text should be readable and not rely on color alone.

## Dependencies

<RegistryDependencies registryName="app-top-bar" />
