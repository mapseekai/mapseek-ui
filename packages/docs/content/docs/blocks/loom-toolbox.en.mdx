---
title: "LoomToolbox"
registryName: loom-toolbox
category: block
stability: experimental
showcase: loom-toolbox
---

import { ShowcaseDemo } from "@/components/ShowcaseDemo"
import { RegistryDependencies, RegistryInstall } from "@/components/RegistryItem"

LoomToolbox is a controlled spatial-analysis toolbox with search, favorites, recents, quick access, parameter details, and run status.

## Installation

<RegistryInstall registryName="loom-toolbox" />

## Import

```tsx
import { LoomToolbox, type LoomTool } from "@/components/blocks/loom-toolbox"
```

## Editing toolbox

<ShowcaseDemo registryName="loom-toolbox" title="Editing toolbox" description="Move between the tool list and parameter details while keeping favorites and recents." minHeight={620} />

## State boundary

The caller owns tool data, filtering, favorites, recents, the active tool, and run results. The component owns only the interaction structure and accessible states.

## Dependencies

<RegistryDependencies registryName="loom-toolbox" />
