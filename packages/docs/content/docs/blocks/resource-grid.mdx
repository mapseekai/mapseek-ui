---
title: "ResourceGrid"
registryName: resource-grid
category: block
stability: stable
showcase: resource-grid
---

import { ShowcaseDemo } from "@/components/ShowcaseDemo"
import { RegistryDependencies, RegistryInstall } from "@/components/RegistryItem"


ResourceGrid 是资源库主区域，按 icon、sprite 和 font 三种 tab 渲染不同密度的卡片布局。

## 安装

<RegistryInstall registryName="resource-grid" />

## 导入

```tsx
import {
  ResourceGrid,
  type ResourceGridItem,
  type ResourceGridProps,
  type ResourceTab,
} from "@/registry/blocks/resource-grid"
```

## 资源卡片、选择与空态

<ShowcaseDemo registryName="resource-grid" title="资源卡片、选择与空态" description="示例复用同一个 TSX 模块作为预览和源码，覆盖原示例的核心状态与交互。" minHeight={620} />

## 用法

传入当前 `tab`、对应 `items` 和打开/右键回调。卡片支持菜单键或 `Shift+F10` 调用右键回调；icon 模式可传 `selectedIconIds` 与 `onIconSelect` 支持批量选择。

## 公开导出与 Props

导出 `ResourceGrid`、`ResourceGridItem`、三类 item 类型、`ResourceStatus`、`ResourceTab`、labels 和 props 类型。

## 加载、空态、错误与状态

空态通过 `empty` slot 注入；加载和错误由外层切换。示例覆盖 tab、选择、打开、右键菜单和空态。

## 无障碍

卡片是按钮，icon 选择复选框应提供 `iconSelectionLabel`，并会在触屏设备上保持可见。卡片名称和元数据会截断，但完整值仍可通过可访问名称、适用位置的原生 `title` 提示或详情页获取。

## 依赖

<RegistryDependencies registryName="resource-grid" />
