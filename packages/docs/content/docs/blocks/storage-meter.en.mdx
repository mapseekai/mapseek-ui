---
title: "StorageMeter"
registryName: storage-meter
category: block
stability: stable
showcase: storage-meter
---

import { ShowcaseDemo } from "@/components/ShowcaseDemo"
import { RegistryDependencies, RegistryInstall } from "@/components/RegistryItem"


StorageMeter is a local storage usage chip with an expanded breakdown for OPFS, IndexedDB, Caches, and related buckets.

## Installation

<RegistryInstall registryName="storage-meter" />

## Import

```tsx
import {
  StorageMeter,
  formatBytes,
  type StorageMeterData,
  type StorageMeterLabels,
  type StorageMeterProps,
} from "@/registry/blocks/storage-meter"
```

## Storage usage and detail popover

<ShowcaseDemo registryName="storage-meter" title="Storage usage and detail popover" description="The preview and source share the same TSX module and show the core states and interactions." minHeight={460} />

## Usage

Shape `navigator.storage.estimate()` or backend quota results as `StorageMeterData`, then pass labels, refresh callback, and optional footer.

## Public Exports and Props

Exports include `StorageMeter`, `formatBytes`, `StorageMeterData`, `StorageMeterLabels`, and props. Key props are `data`, `loading`, `error`, `errorLabel`, `onRefresh`, and `footer`.

## Loading, Empty, Error, and Status

Supports normal, near-limit, unsupported, loading refresh, and error messaging. The demo covers normal, high-watermark, and unsupported data.

## Accessibility

The trigger aria-label includes used storage, quota, and percentage; the unsupported branch explains the reason through a tooltip.

## Dependencies

<RegistryDependencies registryName="storage-meter" />

