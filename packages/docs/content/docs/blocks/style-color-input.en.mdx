---
title: "StyleColorInput"
registryName: style-color-input
category: block
stability: stable
showcase: style-color-input
---

import { ShowcaseDemo } from "@/components/ShowcaseDemo"
import { RegistryDependencies, RegistryInstall } from "@/components/RegistryItem"


StyleColorInput builds on the foundational `ColorInput` and provides compatibility names and exports for style-domain code.

## Installation

<RegistryInstall registryName="style-color-input" />

## Import

```tsx
import {
  StyleColorInput,
  type StyleColorInputProps,
} from "@/registry/blocks/style-color-input"
```

## Color Input Modes

<ShowcaseDemo registryName="style-color-input" title="Color input modes" description="The demo covers button, input, combined layouts, and a replaceable picker." minHeight={440} />

## Usage

Use `value`, `open`, `onTextChange`, and `onOpenChange` for fully controlled input. Pass `renderPicker` for product-specific pickers and call `close` when the picker action is complete.

## Public Exports and Props

Exports include `StyleColorInput`, the lower-level `ColorPicker` parts, and compatibility types such as `StyleColorInputMode`, `StyleColorInputLayout`, and `StyleColorInputProps`; the implementation comes from the foundational `ColorInput`.

## Loading, Empty, Error, and Status

The block does not validate color syntax. Callers should validate before saving and expose invalid input through error text or status messaging.

## Accessibility

Pass `aria-label` for the text field and `swatchLabel` for the swatch trigger. After the popover closes, focus should remain in the same editing context.

## Dependencies

<RegistryDependencies registryName="style-color-input" />
