---
title: "Toolbar"
registryName: toolbar
category: block
stability: experimental
showcase: toolbar
---

import { ShowcaseDemo } from "@/components/ShowcaseDemo"
import { RegistryDependencies, RegistryInstall } from "@/components/RegistryItem"

Toolbar 是地图编辑会话的受控工具条，组合编辑开关、工具组、吸附、保存与<span className="whitespace-nowrap">撤销/重做</span>状态。

## 安装

<RegistryInstall registryName="toolbar" />

## 导入

```tsx
import { Toolbar, type ToolbarGroup } from "@/components/blocks/toolbar"
```

## 编辑工具条

<ShowcaseDemo registryName="toolbar" title="编辑工具条" description="工具可按业务分组，并通过 editOnly 门控编辑态专用操作。" minHeight={560} />

## 工具模型

每个工具提供 `id`、`label`、`icon` 和可选快捷键；`editOnly` 工具会在非编辑状态禁用。业务层负责保存、历史记录与地图交互。

## 依赖

<RegistryDependencies registryName="toolbar" />
