---
id: layer-panel
slug: /blocks/layer-panel
registryName: layer-panel
category: block
stability: stable
examples:
  - layer-panel/basic
  - layer-panel/groups
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { LayerPanelBasicDemo } from "@site/src/examples/layer-panel/basic"
import basicSource from "@site/src/examples/layer-panel/basic.tsx?raw"
import { LayerPanelGroupsDemo } from "@site/src/examples/layer-panel/groups"
import groupsSource from "@site/src/examples/layer-panel/groups.tsx?raw"

export const basicLabels = {
  title: "工程图层",
  addLayer: "添加图层",
  style: "样式",
  filter: "过滤器",
  emptyTitle: "暂无图层",
  emptyDescription: "添加图层后会在这里显示。",
  visibilityOnStatus: "已显示",
  visibilityOffStatus: "已隐藏",
  selectedStatus: "已选中",
  locatedStatus: "定位图层",
  tableStatus: "打开属性表",
  removedStatus: "已删除",
  styleOpacity: "不透明度",
  filterMode: "当前过滤条件",
  layerPanel: {
    addLayer: "添加图层",
    point: "点",
    polyline: "线",
    polygon: "面",
    mixed: "混合",
    raster: "栅格",
    features: "要素",
    locate: "定位",
    zoomToLayer: "缩放到图层",
    attributeTable: "属性表",
    delete: "删除",
  },
}

export const groupLabels = {
  title: "分组图层",
  style: "样式",
  filter: "过滤器",
  collapseGroup: "折叠分组",
  expandGroup: "展开分组",
  renameGroup: "重命名",
  saveRename: "保存",
  groupMenu: "菜单",
  zoomGroup: "缩放至分组",
  duplicateGroup: "复制分组",
  selectedStatus: "已选中",
  visibilityOnStatus: "已显示",
  visibilityOffStatus: "已隐藏",
  collapsedStatus: "已折叠分组",
  expandedStatus: "已展开分组",
  renamedStatus: "已重命名分组",
  menuStatus: "已执行菜单",
  layerPanel: basicLabels.layerPanel,
}

# LayerPanel

LayerPanel 是用于地图工程的受控图层面板。它负责标题、列表、行选择、可见性、行操作、空状态和可插拔 section；图层数据、排序、删除、定位、属性表和分组行为由调用方状态拥有。

## 安装

<RegistryInstall registryName="layer-panel" />

## 导入

```tsx
import { LayerPanel, type LayerData } from "@/registry/blocks/layer-panel"
```

## 基础受控面板

<ComponentDemo title="基础受控面板" description="示例保留可见性切换、受控选中、行内操作、样式 section、过滤 section、删除和空状态。" source={basicSource} minHeight={620}>
  <LayerPanelBasicDemo labels={basicLabels} />
</ComponentDemo>

## 分组、重命名和菜单

<ComponentDemo title="分组、重命名和菜单" description="分组模型、折叠、重命名和菜单状态在示例层组合；每个图层行仍由 LayerPanel.Item 渲染。" source={groupsSource} minHeight={680}>
  <LayerPanelGroupsDemo labels={groupLabels} />
</ComponentDemo>

## 受控状态

`LayerPanel` 不保存业务数据。调用方传入 `layers`、`selectedId` 和处理器，并在 `onSelectChange`、`onVisibleChange`、`onReorder`、`onRemove`、`onLocate`、`onOpenTable` 中更新自己的工程状态。`collapsed` 可以受控，也可以通过 `defaultCollapsed` 让面板自己维护折叠状态。

## 图层与分组模型

`LayerData` 至少需要 `id`、`name`、`visible` 和 `geometryType`。`featureCount`、`crsLabel` 和 `flags` 是展示增强字段。分组不是 registry API；如果工程有分组、重命名或分组菜单，把 group model 保留在调用方，再把过滤后的图层传给 `LayerPanel.Item`。

## 必需处理器

只读面板可以只提供 `layers` 和 `selectedId`。如果显示可见性按钮，必须提供 `onVisibleChange`；如果允许拖拽排序，必须提供 `onReorder`；删除、定位和属性表按钮分别由 `onRemove`、`onLocate` 和 `onOpenTable` 控制。处理器缺失时对应操作不会渲染。

## 无障碍标签

通过 `labels` 覆盖 geometry、添加图层、要素、定位、属性表和删除文案。图层行使用图层名作为可访问名称；可见性、定位、属性表和删除是原生 `button`。调用方添加的分组按钮、菜单和重命名输入也需要自己的 `aria-label` 或可读文本。

## 滚动与响应式

给面板设置明确高度或父级滚动边界，列表会在面板内部滚动，并隐藏横向溢出。文档示例使用 `w-full` 和 `max-w-*`，因此在移动宽度下不会撑破 preview；长名称依靠行内 `truncate` 收敛。

## 依赖

<RegistryDependencies registryName="layer-panel" />

## 相关链接

- [LayerPanel registry JSON](/r/layer-panel.json)
- [StylePanel registry JSON](/r/style-panel.json)
- [FilterPanel registry JSON](/r/filter-panel.json)
