---
id: input-group
slug: /components/input-group
registryName: input-group
category: primitive
stability: stable
examples:
  - input-group/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { InputGroupOverviewDemo } from "@site/src/examples/input-group/overview"
import overviewSource from "@site/src/examples/input-group/overview.tsx?raw"

# InputGroup

InputGroup 用于把 input、textarea、图标、单位和内嵌按钮组合到同一个边界内。它适合搜索、坐标、单位值和多行备注。

## 安装

<RegistryInstall registryName="input-group" />

## 导入

```tsx
import { InputGroup, InputGroupAddon, InputGroupInput, InputGroupText } from "@/registry/ui/input-group"
```

## 用法

<ComponentDemo title="输入组合" description="覆盖图标前缀、文字后缀、invalid 和 block-start textarea addon，保留 Showcase 的组合方式。" source={overviewSource} minHeight={380}>
  <InputGroupOverviewDemo />
</ComponentDemo>

## 导出

`input-group.tsx` 导出 `InputGroup`、`InputGroupAddon`、`InputGroupButton`、`InputGroupInput`、`InputGroupText` 和 `InputGroupTextarea`。

## 状态

addon 可使用 `inline-start`、`inline-end`、`block-start` 或 `block-end`。子控件 `aria-invalid` 会提升到 group 边界；禁用子控件会降低整体可用性样式。

## 无障碍

addon 不替代 label。搜索、坐标和备注输入仍应拥有可见 label 或 `aria-label`；单位文本应避免成为唯一的错误提示。

## 依赖

<RegistryDependencies registryName="input-group" />

## 相关链接

- [InputGroup registry JSON](/r/input-group.json)
- [Input registry JSON](/r/input.json)
- [Textarea registry JSON](/r/textarea.json)
