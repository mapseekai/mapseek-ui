---
id: pagination
slug: /components/pagination
registryName: pagination
category: primitive
stability: stable
examples:
  - pagination/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { PaginationOverviewDemo } from "@site/src/examples/pagination/overview"
import overviewSource from "@site/src/examples/pagination/overview.tsx?raw"

# Pagination

Pagination 用于分页导航，保持语义 `nav`、列表结构、当前页标记和上一页/下一页入口。

## 安装

<RegistryInstall registryName="pagination" />

## 导入

```tsx
import {
  Pagination,
  PaginationContent,
  PaginationItem,
  PaginationLink,
} from "@/registry/ui/pagination"
```

## 用法

<ComponentDemo title="分页导航" description="覆盖当前页、上一页、下一页、首尾页和省略号状态，保留原示例的受控页码行为。" source={overviewSource} minHeight={180}>
  <PaginationOverviewDemo />
</ComponentDemo>

## 导出

`pagination.tsx` 导出 `Pagination`、`PaginationContent`、`PaginationItem`、`PaginationLink`、`PaginationPrevious`、`PaginationNext` 和 `PaginationEllipsis`。

## 状态

`PaginationLink isActive` 会设置 `aria-current="page"` 并使用 active 视觉状态。上一页/下一页可根据当前页禁用或限制目标页。

## 无障碍

保留 `nav` 语义和可读链接文本。当前页必须有 `aria-current="page"`；省略号应只表示跳过区间，不应成为唯一导航方式。

## 依赖

<RegistryDependencies registryName="pagination" />

## 相关链接

- [Pagination registry JSON](/r/pagination.json)
- [Button registry JSON](/r/button.json)
