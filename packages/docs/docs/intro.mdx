---
id: intro
slug: /
registryName: theme
category: primitive
stability: stable
examples: []
---

# Mapseek UI

Mapseek UI 是一套面向地图、空间数据和运维界面的 shadcn 源码组件。安装时组件会进入你的应用源码树，方便团队审查、调整和长期维护。

## 安装方式

使用 shadcn 将组件源码安装到项目中，而不是把 Mapseek UI 当作运行时组件包引入。组件源码、样式 token 和依赖声明来自本仓库的 registry 输出。

```bash
bun run registry:build
```

## 双语文档

中文文档是默认站点，不带路径前缀。英文文档发布在 `/en`，并保持相同的结构化元数据，便于后续组件页面做本地化校验。

## 后续内容

后续页面会逐步覆盖 primitive、block、交互示例和安装说明。本页只是文档工作区的入口，不代表一个可安装组件。
