---
id: linked-ref-list
slug: /blocks/linked-ref-list
registryName: linked-ref-list
category: block
stability: stable
examples:
  - linked-ref-list/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { LinkedRefListDemo, enLinkedRefListLabels } from "@site/src/examples/linked-ref-list/overview"
import overviewSource from "@site/src/examples/linked-ref-list/overview.tsx?raw"

# LinkedRefList

LinkedRefList summarizes dataset, mapset, and workflow references so users can see what consumes or derives from a resource.

## Installation

<RegistryInstall registryName="linked-ref-list" />

## Import

```tsx
import {
  LinkedRefList,
  type LinkedRefGroup,
  type LinkedRefKind,
  type LinkedRefListProps,
} from "@/registry/blocks/linked-ref-list"
```

## Linked reference groups

<ComponentDemo title="Linked reference groups" description="The preview and source share the same TSX module and preserve the core Showcase states and interactions." source={overviewSource} minHeight={520}>
  <LinkedRefListDemo labels={enLinkedRefListLabels} />
</ComponentDemo>

## Usage

Pass grouped `LinkedRefGroup[]` plus icons for each kind. Summary cards own counts, while rows show names, IDs, times, and status.

## Public Exports and Props

Exports include `LinkedRefList`, `LinkedRefGroup`, `LinkedRefItem`, `LinkedRefKind`, and `LinkedRefStatusTone`. `openLabel` names pending open/copy actions.

## Loading, Empty, Error, and Status

The list owns only the selected group; data loading, empty groups, and errors are controlled by the caller deciding whether to render this block.

## Accessibility

Summary cards are buttons and support keyboard switching. Status pills include text and structure, not color-only meaning.

## Dependencies

<RegistryDependencies registryName="linked-ref-list" />

## Related Links

- [LinkedRefList registry JSON](/r/linked-ref-list.json)
- [resource-grid registry JSON](/r/resource-grid.json)
- [resource-detail-drawer registry JSON](/r/resource-detail-drawer.json)
- [resource-status registry JSON](/r/resource-status.json)
