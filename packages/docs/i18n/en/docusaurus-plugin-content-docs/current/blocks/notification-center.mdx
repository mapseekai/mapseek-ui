---
id: notification-center
slug: /blocks/notification-center
registryName: notification-center
category: block
stability: stable
examples:
  - notification-center/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { NotificationCenterDemo, enNotificationCenterLabels } from "@site/src/examples/notification-center/overview"
import overviewSource from "@site/src/examples/notification-center/overview.tsx?raw"

# NotificationCenter

NotificationCenter is a header dropdown for processing jobs, source metadata, stream state, and clear actions.

## Installation

<RegistryInstall registryName="notification-center" />

## Import

```tsx
import {
  NotificationCenter,
  type NotificationCenterItem,
  type NotificationCenterLabels,
  type NotificationCenterProps,
} from "@/registry/blocks/notification-center"
```

## Notifications, loading, error, and empty states

<ComponentDemo title="Notifications, loading, error, and empty states" description="The preview and source share the same TSX module and preserve the core Showcase states and interactions." source={overviewSource} minHeight={520}>
  <NotificationCenterDemo labels={enNotificationCenterLabels} />
</ComponentDemo>

## Usage

Pass controlled `items`, localized labels, and `streamActive`. Clear all, clear one, and retry are delegated through callbacks.

## Public Exports and Props

Exports include `NotificationCenter`, `NotificationCenterItem`, `NotificationCenterLabels`, `NotificationCenterStatusTone`, and props. State props include `isLoading`, `isError`, and `streamActive`.

## Loading, Empty, Error, and Status

The block includes loading skeletons, retryable error, empty state, and populated summaries. Demo buttons cover all four paths.

## Accessibility

The trigger uses `labels.trigger` as its accessible name; the dropdown inherits keyboard and focus handling from DropdownMenu.

## Dependencies

<RegistryDependencies registryName="notification-center" />

## Related Links

- [NotificationCenter registry JSON](/r/notification-center.json)
- [dropdown-menu registry JSON](/r/dropdown-menu.json)
- [empty registry JSON](/r/empty.json)
- [service-status registry JSON](/r/service-status.json)
