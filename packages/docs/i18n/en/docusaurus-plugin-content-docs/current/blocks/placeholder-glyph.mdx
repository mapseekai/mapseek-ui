---
id: placeholder-glyph
slug: /blocks/placeholder-glyph
registryName: placeholder-glyph
category: block
stability: stable
examples:
  - placeholder-glyph/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { PlaceholderGlyphDemo, enPlaceholderGlyphLabels } from "@site/src/examples/placeholder-glyph/overview"
import overviewSource from "@site/src/examples/placeholder-glyph/overview.tsx?raw"

# PlaceholderGlyph

PlaceholderGlyph hashes a seed into a stable monochrome SVG for resource grids, drawers, and sprite previews before real icons are uploaded.

## Installation

<RegistryInstall registryName="placeholder-glyph" />

## Import

```tsx
import { PlaceholderGlyph, type PlaceholderGlyphProps } from "@/registry/blocks/placeholder-glyph"
```

## Deterministic resource placeholder glyphs

<ComponentDemo title="Deterministic resource placeholder glyphs" description="The preview and source share the same TSX module and preserve the core Showcase states and interactions." source={overviewSource} minHeight={520}>
  <PlaceholderGlyphDemo labels={enPlaceholderGlyphLabels} />
</ComponentDemo>

## Usage

Pass a stable `seed` for each resource, control pixel size with `size`, and use `mono` for lower visual weight.

## Public Exports and Props

Exports include `PlaceholderGlyph` and `PlaceholderGlyphProps`. Key props are `seed`, `size`, `mono`, and `className`.

## Loading, Empty, Error, and Status

The component has no internal state; callers usually fall back to it when real SVG, sprite, or font previews are missing.

## Accessibility

The SVG includes a title; when used inside buttons or image semantics, provide the specific resource name from the wrapper.

## Dependencies

<RegistryDependencies registryName="placeholder-glyph" />

## Related Links

- [PlaceholderGlyph registry JSON](/r/placeholder-glyph.json)
- [resource-grid registry JSON](/r/resource-grid.json)
- [resource-detail-drawer registry JSON](/r/resource-detail-drawer.json)
- [product-logo registry JSON](/r/product-logo.json)
