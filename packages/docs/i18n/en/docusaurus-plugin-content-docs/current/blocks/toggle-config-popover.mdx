---
id: toggle-config-popover
slug: /blocks/toggle-config-popover
registryName: toggle-config-popover
category: block
stability: stable
examples:
  - toggle-config-popover/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { ToggleConfigPopoverDemo, enToggleConfigPopoverLabels } from "@site/src/examples/toggle-config-popover/overview"
import overviewSource from "@site/src/examples/toggle-config-popover/overview.tsx?raw"

# ToggleConfigPopover

ToggleConfigPopover combines a switch button and settings popover in one toolbar unit for map snapping, measuring, and editing controls.

## Installation

<RegistryInstall registryName="toggle-config-popover" />

## Import

```tsx
import {
  ToggleConfigPopover,
  type ToggleConfigPopoverProps,
} from "@/registry/blocks/toggle-config-popover"
```

## Toggle and Configuration

<ComponentDemo title="Toggle and configuration" description="The demo covers switch state, popover settings, slider, checkbox, and disabled coupling." source={overviewSource} minHeight={420}>
  <ToggleConfigPopoverDemo labels={enToggleConfigPopoverLabels} />
</ComponentDemo>

## Usage

Control `checked` and `onCheckedChange` from the caller, and render threshold, target, or other configuration fields as children. `open` can be controlled or left to internal popover state.

## Public Exports and Props

Exports include `ToggleConfigPopover`, `ToggleConfigPopoverProps`, and labels types. Pass a Tabler icon component as the `icon` prop.

## Loading, Empty, Error, and Status

The component does not handle async saving. Disabled state disables both the main switch and settings trigger. Loading, error, and save status inside the configuration form belong to the child content.

## Accessibility

The main button uses `role="switch"` and `aria-checked`. Provide `toggleLabel`, `triggerLabel`, `switchLabel`, and tooltip text so icon buttons have readable names.

## Dependencies

<RegistryDependencies registryName="toggle-config-popover" />

## Related Links

- [ToggleConfigPopover registry JSON](/r/toggle-config-popover.json)
- [popover registry JSON](/r/popover.json)
- [tooltip registry JSON](/r/tooltip.json)
