---
id: badge
slug: /components/badge
registryName: badge
category: primitive
stability: stable
examples:
  - badge/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { BadgeOverviewDemo } from "@site/src/examples/badge/overview"
import overviewSource from "@site/src/examples/badge/overview.tsx?raw"

# Badge

Badge is a compact marker for state, category, and metadata. Use it for publication status, CRS, format, and error level; do not use it as a replacement for buttons or full help text.

## Installation

<RegistryInstall registryName="badge" />

## Import

```tsx
import { Badge, badgeVariants } from "@/registry/ui/badge"
```

## Variants and States

<ComponentDemo title="Variants and states" description="Preserves the Showcase six variants and GIS status examples." source={overviewSource} minHeight={250}>
  <BadgeOverviewDemo />
</ComponentDemo>

## Exports

`Badge` exports the component and `badgeVariants` exports the class-variance-authority style helper. The component renders a `span` by default and can change host elements through Base UI `render`.

## States and Behavior

`variant` supports `default`, `secondary`, `destructive`, `outline`, `ghost`, and `link`. When rendered as a link, hover styles stay aligned through the `[a]` selectors.

## Accessibility

State text must explain itself; use “Error” rather than only a red color. Clickable badges should render as links or buttons with a clear purpose.

## Dependencies

<RegistryDependencies registryName="badge" />

## Related Links

- [Badge registry JSON](/r/badge.json)
- [Button registry JSON](/r/button.json)
- [Avatar registry JSON](/r/avatar.json)
