---
id: card
slug: /components/card
registryName: card
category: primitive
stability: stable
examples:
  - card/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { CardOverviewDemo } from "@site/src/examples/card/overview"
import overviewSource from "@site/src/examples/card/overview.tsx?raw"

# Card

Card organizes a title, description, content, and actions into one clearly bounded content unit. Use it for dataset summaries, export jobs, and layer details, not nested page-level containers.

## Installation

<RegistryInstall registryName="card" />

## Import

```tsx
import { Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/registry/ui/card"
```

## Content Structure

<ComponentDemo title="Content structure" description="Preserves the Showcase basic card, content with footer, and action slot." source={overviewSource} minHeight={380}>
  <CardOverviewDemo />
</ComponentDemo>

## Exports

`Card` is the root; `CardHeader`, `CardTitle`, `CardDescription`, `CardAction`, `CardContent`, and `CardFooter` define common regions. `CardBody` is an alias for `CardContent`.

## States and Behavior

`size="sm"` tightens spacing. `CardAction` aligns to the right side of the header grid, and `CardFooter` adds the top border for action groups.

## Accessibility

Card has no interaction semantics by itself. If the whole card is clickable, use a link or button for the interaction and keep title, description, and target clear.

## Dependencies

<RegistryDependencies registryName="card" />

## Related Links

- [Card registry JSON](/r/card.json)
- [Badge registry JSON](/r/badge.json)
- [Button registry JSON](/r/button.json)
