---
id: combobox
slug: /components/combobox
registryName: combobox
category: primitive
stability: stable
examples:
  - combobox/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { ComboboxOverviewDemo } from "@site/src/examples/combobox/overview"
import overviewSource from "@site/src/examples/combobox/overview.tsx?raw"

# Combobox

Combobox is a searchable single-value selector for formats, coordinate systems, or resources. It composes input-group, a portal list, empty state, clear button, and trigger button.

## Installation

<RegistryInstall registryName="combobox" />

## Import

```tsx
import { Combobox, ComboboxContent, ComboboxInput, ComboboxItem, ComboboxList } from "@/registry/ui/combobox"
```

## Usage

<ComponentDemo title="Searchable selection" description="Preserves the reference example format picker, CRS picker, filtering, clear, trigger, and empty state." source={overviewSource} minHeight={300}>
  <ComboboxOverviewDemo />
</ComponentDemo>

## Exports

`combobox.tsx` exports `Combobox`, `ComboboxInput`, `ComboboxContent`, `ComboboxList`, `ComboboxItem`, `ComboboxEmpty`, `ComboboxGroup`, `ComboboxLabel`, `ComboboxValue`, `ComboboxTrigger`, `ComboboxClear`, and chips components.

## States

Use `value/onValueChange` to control the input and selected value. Render `ComboboxEmpty` when filtering returns no options. `showTrigger` renders the dropdown button, `showClear` renders the clear button, and `disabled` passes to the input and buttons.

## Accessibility

Give the input a clear `aria-label` or visible label. Items need stable `value` props; keyboard users should be able to use typing, arrow keys, Enter, and Escape.

## Dependencies

<RegistryDependencies registryName="combobox" />

## Related Links

- [Combobox registry JSON](/r/combobox.json)
- [InputGroup registry JSON](/r/input-group.json)
- [Input registry JSON](/r/input.json)
