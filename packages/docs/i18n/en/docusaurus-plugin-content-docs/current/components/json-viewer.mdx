---
id: json-viewer
slug: /components/json-viewer
registryName: json-viewer
category: primitive
stability: stable
examples:
  - json-viewer/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { JsonViewerOverviewDemo } from "@site/src/examples/json-viewer/overview"
import overviewSource from "@site/src/examples/json-viewer/overview.tsx?raw"

# JsonViewer

JsonViewer displays structured JSON in docs, debug panels, and data detail views. It supports node collapse, expand all, collapse all, copy, and optional colored indentation.

## Installation

<RegistryInstall registryName="json-viewer" />

## Import

```tsx
import { JsonViewer } from "@/registry/ui/json-viewer"
```

## GeoJSON Preview

<ComponentDemo title="GeoJSON preview" description="Preserves the Showcase title, default expansion, and colored indent, with expand, collapse, and copy interactions." source={overviewSource} minHeight={360}>
  <JsonViewerOverviewDemo />
</ComponentDemo>

## Exports

`json-viewer.tsx` exports `JsonViewer`. It accepts `data`, `title`, `defaultExpanded`, `showLineNumbers`, `showColorIndent`, `collapseOn`, and copy-feedback props.

## States and Behavior

`defaultExpanded` can be a boolean or expansion depth number. Object and array nodes can be collapsed, and toolbar buttons expand all, collapse to the root, or copy formatted JSON.

## Accessibility

Give the JSON region a clear title. Copy controls need readable labels, and deeply nested data should have nearby context for source and important fields.

## Dependencies

<RegistryDependencies registryName="json-viewer" />

## Related Links

- [JsonViewer registry JSON](/r/json-viewer.json)
- [Collapsible registry JSON](/r/collapsible.json)
- [Button registry JSON](/r/button.json)
