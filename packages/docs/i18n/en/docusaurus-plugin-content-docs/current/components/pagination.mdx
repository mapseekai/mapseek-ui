---
id: pagination
slug: /components/pagination
registryName: pagination
category: primitive
stability: stable
examples:
  - pagination/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { PaginationOverviewDemo } from "@site/src/examples/pagination/overview"
import overviewSource from "@site/src/examples/pagination/overview.tsx?raw"

# Pagination

Pagination provides page navigation with semantic `nav`, list structure, current-page state, and previous/next controls.

## Installation

<RegistryInstall registryName="pagination" />

## Import

```tsx
import {
  Pagination,
  PaginationContent,
  PaginationItem,
  PaginationLink,
} from "@/registry/ui/pagination"
```

## Usage

<ComponentDemo title="Page navigation" description="Covers current page, previous, next, first and last links, and ellipsis while preserving the reference example controlled page behavior." source={overviewSource} minHeight={180}>
  <PaginationOverviewDemo />
</ComponentDemo>

## Exports

`pagination.tsx` exports `Pagination`, `PaginationContent`, `PaginationItem`, `PaginationLink`, `PaginationPrevious`, `PaginationNext`, and `PaginationEllipsis`.

## States

`PaginationLink isActive` sets `aria-current="page"` and active visual state. Previous and next links can clamp or disable based on the current page.

## Accessibility

Keep the `nav` semantics and readable link text. The current page must use `aria-current="page"`; ellipsis should represent skipped ranges, not the only way to navigate.

## Dependencies

<RegistryDependencies registryName="pagination" />

## Related Links

- [Pagination registry JSON](/r/pagination.json)
- [Button registry JSON](/r/button.json)
