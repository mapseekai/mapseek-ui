---
id: skeleton
slug: /components/skeleton
registryName: skeleton
category: primitive
stability: stable
examples:
  - skeleton/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { SkeletonOverviewDemo } from "@site/src/examples/skeleton/overview"
import overviewSource from "@site/src/examples/skeleton/overview.tsx?raw"

# Skeleton

Skeleton is a loading placeholder for known content structure while data is still unavailable. It fits text rows, card content, and list placeholders.

## Installation

<RegistryInstall registryName="skeleton" />

## Import

```tsx
import { Skeleton } from "@/registry/ui/skeleton"
```

## Loading Placeholders

<ComponentDemo title="Loading placeholders" description="Preserves the Showcase text-row skeletons and card skeleton." source={overviewSource} minHeight={280}>
  <SkeletonOverviewDemo />
</ComponentDemo>

## Exports

`Skeleton` is a `div` with `data-slot="skeleton"` and standard `div` props. Callers control dimensions through `className`.

## States and Behavior

The default style uses `animate-pulse` and `bg-muted`. Placeholder dimensions should approximate final content to avoid layout shift after loading.

## Accessibility

Skeleton should not be announced as real content. Long loading regions need parent-level `aria-busy` or status text so screen-reader users know work is in progress.

## Dependencies

<RegistryDependencies registryName="skeleton" />

## Related Links

- [Skeleton registry JSON](/r/skeleton.json)
- [Progress registry JSON](/r/progress.json)
- [Empty registry JSON](/r/empty.json)
