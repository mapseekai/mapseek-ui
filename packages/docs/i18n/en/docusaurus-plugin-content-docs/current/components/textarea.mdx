---
id: textarea
slug: /components/textarea
registryName: textarea
category: primitive
stability: stable
examples:
  - textarea/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { TextareaOverviewDemo } from "@site/src/examples/textarea/overview"
import overviewSource from "@site/src/examples/textarea/overview.tsx?raw"

# Textarea

Textarea handles multiline short text such as notes, descriptions, and error details. It keeps Mapseek input boundaries, focus ring, invalid state, and disabled semantics.

## Installation

<RegistryInstall registryName="textarea" />

## Import

```tsx
import { Textarea } from "@/registry/ui/textarea"
```

## Usage

<ComponentDemo title="Multiline input states" description="Covers controlled, readOnly, disabled, and invalid while preserving the reference example default/disabled/error states." source={overviewSource} minHeight={460}>
  <TextareaOverviewDemo />
</ComponentDemo>

## Exports

`textarea.tsx` exports `Textarea`. The component inherits native `textarea` props, including `value/onChange`, `defaultValue`, `readOnly`, `disabled`, `aria-invalid`, and `rows`.

## States

Controlled input uses `value/onChange`; read-only text remains selectable and copyable; disabled means the field cannot be edited. Invalid state uses `aria-invalid`.

## Accessibility

Textarea needs a visible label or `aria-label`. Long help or error copy should live in `FieldDescription`, `FieldError`, or adjacent text.

## Dependencies

<RegistryDependencies registryName="textarea" />

## Related Links

- [Textarea registry JSON](/r/textarea.json)
- [InputGroup registry JSON](/r/input-group.json)
- [Field registry JSON](/r/field.json)
