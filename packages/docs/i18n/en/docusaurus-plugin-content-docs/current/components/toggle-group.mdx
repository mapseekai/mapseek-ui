---
id: toggle-group
slug: /components/toggle-group
registryName: toggle-group
category: primitive
stability: stable
examples:
  - toggle-group/overview
---

import { ComponentDemo } from "@site/src/components/ComponentDemo"
import { RegistryDependencies, RegistryInstall } from "@site/src/components/RegistryItem"
import { ToggleGroupOverviewDemo } from "@site/src/examples/toggle-group/overview"
import overviewSource from "@site/src/examples/toggle-group/overview.tsx?raw"

# ToggleGroup

ToggleGroup combines ToggleGroupItem controls into single-select or multi-select groups for alignment, text style, map tool modes, and related options.

## Installation

<RegistryInstall registryName="toggle-group" />

## Import

```tsx
import { ToggleGroup, ToggleGroupItem } from "@/registry/ui/toggle-group"
```

## Usage

<ComponentDemo title="Toggle group states" description="Covers controlled single select, multi select, disabled item, and state readout while preserving the reference example alignment/styles groups." source={overviewSource} minHeight={340}>
  <ToggleGroupOverviewDemo />
</ComponentDemo>

## Exports

`toggle-group.tsx` exports `ToggleGroup` and `ToggleGroupItem`. The root supports `value/onValueChange`, `variant`, `size`, `spacing`, and `orientation`.

## States

`value` is a string array, which can represent single or multiple selections. `spacing=0` can form a continuous outline group; disabled items ignore pointer and keyboard activation.

## Accessibility

Each item should name the option. Keyboard users should be able to move within the group and toggle with Enter/Space; state readout should not rely on background color alone.

## Dependencies

<RegistryDependencies registryName="toggle-group" />

## Related Links

- [ToggleGroup registry JSON](/r/toggle-group.json)
- [Toggle registry JSON](/r/toggle.json)
- [Button registry JSON](/r/button.json)
