import type { StylePanelLabels } from "./labels"

export const DEFAULT_STYLE_PANEL_LABELS = {
  fill: "填充",
  fillColor: "填充",
  opacity: "透明度",
  stroke: "描边",
  marker: "符号",
} satisfies StylePanelLabels
