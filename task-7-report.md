# Task 7 Fix Report

## Scope

- Move LayerPanel zh/en labels out of MDX and into the example source modules so `?raw` source is reproducible.
- Strengthen grouped LayerPanel pilots QA viewport and horizontal overflow assertions after each interaction.

## Ignored / Unstaged Paths

- `packages/docs/.docusaurus/`
- `packages/docs/build/`
- existing registry migration edits under `registry/`
- existing Showcase migration files under `showcase/`
- existing legacy `src/` deletions
- existing root/config dirty files such as `README.md`, `biome.json`, `bun.lock`, `package.json`, and `tsconfig.json`

## Commit Policy

- This report is intentionally uncommitted.
- Only Task7 fix files should be staged for the Lore fix commit.
